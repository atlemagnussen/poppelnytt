{
  "devDependencies": {
    "vitepress": "1.6.3"
  },
  "scripts": {
    "start": "vitepress dev press",
    "build": "vitepress build press",
    "preview": "vitepress preview press",
    "deploy:firebase": "firebase deploy --only hosting:poppelnytt",
    "deploy": "npm run build && npm run deploy:firebase"
  },
  "dependencies": {
    "@cynber/vitepress-valence": "^0.2.2",
    "pagefind": "^1.4.0",
    "vitepress-plugin-pagefind": "^0.4.17"
  }
}
